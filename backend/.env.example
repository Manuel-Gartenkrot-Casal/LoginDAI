# Server
PORT=3001
FRONTEND_URL=http://localhost:3000

# PostgreSQL (match your pgAdmin config)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=auth_db
DB_USER=postgres
DB_PASSWORD=your_pgadmin_password

# JWT — change this to a long random string in production
JWT_SECRET=super_secret_jwt_key_change_me_in_production_2024
